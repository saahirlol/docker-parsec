#!/bin/bash
echo "j"

wget https://builds.parsec.app/package/parsec-linux.deb -O parsec.deb
sudo apt-get install -y ./parsec.deb
rm parsec.deb

# Find and remove Singleton files in all profile directories



bash /usr/bin/parsec
